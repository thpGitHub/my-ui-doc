---
title: "DangerButton"
---

import DangerButton from '../../../components/DangerButton.jsx';
import { Code } from '@astrojs/starlight/components';

<DangerButton />

## Import

<Code code={`import { DangerButton } from 'my-react-ui-components';`} lang="bash" />

## Code

<Code code={`import React, { useState } from 'react';

const DangerButton = ({ children = 'DangerButton' }) => {
  const [hover, setHover] = useState(false);

  const buttonStyle = {
    padding: '10px 20px',
    border: 'none',
    borderRadius: '5px',
    cursor: 'pointer',
    fontSize: '16px',
    margin: '5px',
    backgroundColor: hover ? 'white' : 'red',
    color: hover ? 'red' : 'white',
    transition: 'background-color 0.3s ease, color 0.3s ease',
  };

  return (
    <button
      style={buttonStyle}
      onMouseEnter={() => setHover(true)}
      onMouseLeave={() => setHover(false)}
    >
      {children}
    </button>
  );
};

export default DangerButton;
`} lang="js" title="DangerButton.jsx" />